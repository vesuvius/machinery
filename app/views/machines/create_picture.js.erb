<% if @picture.new_record? %>
	alert("Failed to upload image: <%= j @picture.errors.full_messages.join(', ').html_safe %>");
<% else %>
	$("#pictures").append("<%= j render(partial: 'picture', object: @picture) %>");
<% end %>